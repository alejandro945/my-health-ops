apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: runners
  annotations:
      prometheus.io/scrape: 'true'
      prometheus.io/path:   /
      prometheus.io/port:   '8080'
spec:
  type: LoadBalancer
  loadBalancerIP: 52.149.201.78
  selector:
    app: jenkins-server
  ports:
    - port: 8080
      targetPort: 8080
      # nodePort: 32000